% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/steps.R
\name{abm_sequential}
\alias{abm_sequential}
\title{Update agent columns one agent at a time}
\usage{
abm_sequential(..., .order = NULL)
}
\arguments{
\item{...}{One or more \code{column ~ expression} rules. The left-hand side may
name either an agent column or a global.}

\item{.order}{Optional expression, evaluated over the whole population,
whose ascending order is the order agents are processed in. The default is a
fresh shuffle every step, which is right when the order is meant to be
arbitrary and wrong when it is part of the model: a queue at a counter, a
sequential-service constraint, a fixed speaking order. \code{NA} sits the agent
out of the step.}
}
\value{
An \code{abm_sequential} step object.
}
\description{
\code{abm_sequential()} is the order-dependent sibling of \code{\link[=abm_rules]{abm_rules()}}. Agents
are processed one at a time in a freshly shuffled order, and each agent's
writes to \strong{globals} are visible to every agent processed after it within
the same step. This mirrors NetLogo's \verb{ask turtles}, and it is what you need
when agents compete for a shared resource that is \emph{depleted} rather than
merely divided, a bank's lendable reserves, say.
}
\details{
Rules also cascade \emph{within} each agent: the second rule sees what the first
one just wrote, to the agent's own row and to the globals alike. That is the
opposite of \code{\link[=abm_rules]{abm_rules()}}, where every rule reads the state at the start of
the step, and it is what "one agent at a time" already implies, an agent
that draws a quote and then decides whether the quote crosses the book has
to be able to read the number it just drew.

During the per-agent loop a rule can read and write its own agent's columns
and any global. If an \code{\link[=abm_match]{abm_match()}} is standing it can also read and write
its \strong{partner's}: the step is narrowed to the agents the match placed in a
group, \code{.partner} and \verb{partner_<col>} come into scope, and a rule whose
left-hand side is \verb{partner_<col>} writes into that agent's row. Because the
partner is read live rather than copied at the start of the step, the
second buyer at a shop sees the stock the first one took, which is what a
decentralised market is and what \code{\link[=abm_tell]{abm_tell()}} cannot say: \code{abm_tell()}
resolves every sender at once, so a stock it draws down can go negative.

It reaches no further than that. To write into a row that is neither its own
nor its partner's, use \code{\link[=abm_tell]{abm_tell()}}. Use \code{\link[=abm_rules]{abm_rules()}} unless you
specifically need the ordering, since sequential evaluation is both slower
and harder to reason about.
}
\examples{
abm_sequential(
  loan          ~ ifelse(wallet < 0, loan + 1, loan),
  bank_reserves ~ ifelse(wallet < 0, bank_reserves - 1, bank_reserves)
)

# a sale: what is left on the shelf is what the customers before me left
abm_sequential(
  got         ~ pmin(want, money / partner_price, partner_stock),
  money       ~ money - got * partner_price,
  partner_stock ~ partner_stock - got
)
}
\seealso{
\code{\link[=abm_go]{abm_go()}}, which lists every step and fixes the order they run
in.

Other agent update steps:
\code{\link[=abm_global]{abm_global()}},
\code{\link[=abm_move]{abm_move()}},
\code{\link[=abm_neighbours]{abm_neighbours()}},
\code{\link[=abm_rules]{abm_rules()}},
\code{\link[=abm_tell]{abm_tell()}}
}
\concept{agent update steps}
