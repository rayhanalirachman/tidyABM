% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repeat.R
\name{abm_repeat}
\alias{abm_repeat}
\title{Repeat a block of steps until a condition holds}
\usage{
abm_repeat(..., until = NULL, max)
}
\arguments{
\item{...}{Step objects, validated as \code{\link[=abm_go]{abm_go()}} validates them.}

\item{until}{A condition checked after each pass. Must collapse to one
logical value. \code{NULL} (the default) means always run \code{max} times.}

\item{max}{The maximum number of passes. Required.}
}
\value{
An \code{abm_repeat} step object.
}
\description{
A tick is normally one pass through \code{\link[=abm_go]{abm_go()}}. Some models have a \emph{phase}
inside the tick that has to run to completion before the next phase starts:
an epidemic that burns out before anyone reconsiders whether to vaccinate, a
round of proposals that continues until nobody is rejected, a market that
clears. \code{abm_repeat()} is that phase. It holds a block of steps and replays
it until \code{until} is true, or \code{max} times, whichever comes first.
}
\details{
\code{until} is evaluated the way an \code{\link[=abm_global]{abm_global()}} right-hand side is, over the
whole population, with the globals in scope, and must collapse to a single
\code{TRUE} or \code{FALSE}. It is checked \emph{after} each pass, so the block always runs
at least once. \code{max} is required, because a condition that never becomes
true would otherwise hang the run.

The same idea covers early stopping for a whole model: a block wrapped in
\verb{abm_repeat(until = <absorbed>, max = <ticks>)} and run for a single tick
stops as soon as the model reaches its absorbing state, instead of
recomputing a fixed point for the rest of the run.
}
\examples{
# an epidemic that burns out before the tick ends
abm_repeat(
  abm_neighbours(exposure ~ sum(state == "I")),
  abm_rules(state ~ ifelse(state == "I", "R", state)),
  until = sum(state == "I") == 0,
  max = 500
)
}
\seealso{
\code{\link[=abm_go]{abm_go()}}, which lists every step and fixes the order they run
in. The block it wraps is validated exactly as \code{\link[=abm_go]{abm_go()}} validates a
tick.
}
