% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-steps.R
\name{abm_neighbours}
\alias{abm_neighbours}
\title{Summarise each agent's network neighbours}
\usage{
abm_neighbours(...)
}
\arguments{
\item{...}{One or more \code{column ~ aggregate_expression} rules. The expression
sees the neighbours' agent columns, the focal agent's own columns as
\verb{own_<col>}, and any global.}
}
\value{
An \code{abm_neighbours} step object.
}
\description{
A match gives an agent \emph{one} partner. Plenty of models need the whole
neighbourhood instead — how many of my neighbours are infected, what fraction
of them are flashing, what my neighbours believe on average.
\code{abm_neighbours()} writes exactly that: for every agent, an aggregate over the
agents it is connected to in the model's \code{\link[=abm_network]{abm_network()}}.
}
\details{
Each rule is \code{column ~ aggregate_expression}, and the expression is evaluated
over the neighbours' rows, so \code{sum(infected)} means "how many of my neighbours
are infected" and \code{mean(opinion)} means "what my neighbours think on average".
An agent with no neighbours gets \code{NA}.

Alongside each neighbour column the expression also sees \verb{own_<col>}, the
focal agent's own value of that column, recycled down its neighbourhood. That
is what makes a \emph{comparison} possible — \code{sum(wealth > own_wealth)} is "how many
of my neighbours are richer than me", which no aggregate over the neighbours
alone can express.
}
\examples{
abm_neighbours(infected_neighbours ~ sum(state == "infected"))
abm_neighbours(richer_neighbours ~ sum(wealth > own_wealth))
}
