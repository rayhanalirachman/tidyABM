# Generate an ODD protocol skeleton

The ODD protocol (Overview, Design concepts, Details – Grimm et al.
2006; 2010; 2020, *JASSS* 23(2):7) is the template a paper describes an
ABM in. `abm_odd()` fills in the half of it that the model objects
already contain and marks the other half for you.

## Usage

``` r
abm_odd(model, go, ticks = NULL)
```

## Arguments

- model:

  An `abm_model` from
  [`abm_setup()`](https://rayhanalirachman.github.io/tidyABM/reference/abm_setup.md).

- go:

  An `abm_go` sequence.

- ticks:

  Optional number of ticks, used for element 2's temporal scale. Pass
  the same number you pass
  [`abm_run()`](https://rayhanalirachman.github.io/tidyABM/reference/abm_run.md).

## Value

An `abm_odd` object: a named list with one entry per ODD element, each
holding structured fields.
[`print()`](https://rdrr.io/r/base/print.html) renders it as markdown
and [`format()`](https://rdrr.io/r/base/format.html) returns that
markdown as a character vector.

## Details

It can do that because a tidyABM model is data:
[`abm_setup()`](https://rayhanalirachman.github.io/tidyABM/reference/abm_setup.md)
returns the population and
[`abm_go()`](https://rayhanalirachman.github.io/tidyABM/reference/abm_go.md)
returns an ordered list of typed steps, so the entities and their state
variables (element 2), the schedule (element 3), the initialisation
(element 5) and the submodels' equations (element 7) can be read off
them rather than remembered. Three of element 4's design concepts –
interaction, sensing and stochasticity – are mechanical too, and are
likewise derived.

Everything else is left as `[author input required]`. Purpose,
emergence, adaptation, objectives, learning, prediction, collectives,
observation and the *justification* for each submodel are statements
about what the modeller meant, and no object in the package records
intent. In particular this function never writes that a concept is
absent: "no adaptation" is a claim, and it is yours to make.

Two things follow from that and are worth knowing before you paste the
output into a paper. Expressions are printed verbatim, never restated in
English, because element 7 wants the equations. And nothing here
describes *behaviour*: ODD is the specification, not the results, so no
derived text says what a run does.

## See also

[`abm_setup()`](https://rayhanalirachman.github.io/tidyABM/reference/abm_setup.md)
and
[`abm_go()`](https://rayhanalirachman.github.io/tidyABM/reference/abm_go.md),
the two objects this reads.

## Examples

``` r
economy <- abm_setup(agents = abm_agents(n = 500, money = 100))
go <- abm_go(
  abm_match(pair = "random", role = list(giver = money > 0, receiver = TRUE)),
  abm_rules(money ~ if_else(.role == "giver", money - 1, money + 1))
)
abm_odd(economy, go, ticks = 1000)
#> # ODD protocol
#> 
#> Generated by `abm_odd()` from the model and its `abm_go()` block. Everything marked **[author input required]** is a statement about intent, which no object in the package records; fill those in yourself.
#> 
#> ## 1. Purpose and patterns
#> 
#> The purpose of this model: **[author input required]**.
#> 
#> The patterns that make it useful for that purpose: **[author input required]**.
#> 
#> ## 2. Entities, state variables and scales
#> 
#> ### Entities and state variables
#> 
#> **agents** -- 500 agents.
#> 
#> | state variable | type |
#> | --- | --- |
#> | `money` | numeric |
#> 
#> ### Global variables
#> 
#> The model declares no global variables.
#> 
#> ### Spatial extent
#> 
#> The model declares neither a network nor a lattice, so it represents no space.
#> 
#> ### Temporal extent
#> 
#> One simulation is 1000 ticks.
#> What one tick represents in the modelled system: **[author input required]**.
#> 
#> ## 3. Process overview and scheduling
#> 
#> Each tick replays 2 processes, in this order.
#> 
#> 1. **Interaction** (`abm_match`)
#>    - mode: `random`, mutual, in groups of 2
#>    - roles: `giver`, `receiver`
#> 2. **Update** (`abm_rules`)
#>    - evaluated over: matched
#>    - writes: `money`
#>    - reads: `.role`, `money`
#>    - all rules are evaluated against the state at the start of the step
#> 
#> ## 4. Design concepts
#> 
#> ### Basic principles
#> 
#> **[author input required]**
#> 
#> ### Emergence
#> 
#> **[author input required]**
#> 
#> ### Adaptation
#> 
#> **[author input required]**
#> 
#> ### Objectives
#> 
#> **[author input required]**
#> 
#> ### Learning
#> 
#> **[author input required]**
#> 
#> ### Prediction
#> 
#> **[author input required]**
#> 
#> ### Sensing
#> 
#> Derived from every expression in the go block.
#> 
#> - Own state variables: `money`
#> - Reserved columns: `.role`
#> 
#> No rule reads a `partner_` column, so no agent's expression depends on the state of the agent it is matched with.
#> 
#> ### Interaction
#> 
#> Direct interaction is mediated by matching:
#> - process 1: `random` pairing, mutual, in groups of 2, roles `giver`, `receiver`
#> 
#> What the interaction represents: **[author input required]**.
#> 
#> ### Stochasticity
#> 
#> The model draws in the following places:
#> - process 1 -- agents are shuffled into groups (pairing)
#> 
#> Draws are found by reading the declared expressions, so a draw made inside a function called from a rule is not listed here. Check this list against any helper the model calls.
#> 
#> Why each is modelled as random: **[author input required]**.
#> 
#> ### Collectives
#> 
#> **[author input required]**
#> 
#> ### Observation
#> 
#> **[author input required]**
#> 
#> ## 5. Initialisation
#> 
#> **agents** -- 500 agents at tick 0.
#> 
#> - `money`: every agent starts at `100`
#> 
#> Whether these values are arbitrary or taken from data: **[author input required]**.
#> 
#> ## 6. Input data
#> 
#> Whether the model uses external data as input: **[author input required]**.
#> 
#> ## 7. Submodels
#> 
#> Each process of element 3, with the expressions it is declared by, verbatim.
#> 
#> ### 1. `abm_match`
#> 
#> ```r
#> pair = "random"
#> role = list(giver = money > 0, receiver = TRUE)
#> ```
#> 
#> Justification and parameter provenance: **[author input required]**.
#> 
#> ### 2. `abm_rules`
#> 
#> ```r
#> money ~ if_else(.role == "giver", money - 1, money + 1)
#> ```
#> 
#> Justification and parameter provenance: **[author input required]**.
#> 
```
