% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{abm_setup}
\alias{abm_setup}
\title{Set up a model}
\usage{
abm_setup(agents, network = NULL, globals = list(), seed = NULL)
}
\arguments{
\item{agents}{Either one \code{\link[=abm_agents]{abm_agents()}} specification, or a \emph{named} list of
them for a model with several kinds of agent (for example
\code{list(buyers = abm_agents(...), sellers = abm_agents(...))}).}

\item{network}{Optionally an \code{\link[=abm_network]{abm_network()}} specification.}

\item{globals}{A named list of population-level values shared by every agent,
for example \code{list(last_attendance = 60)}. Globals are readable inside every
rule and are updated by \code{\link[=abm_global]{abm_global()}}.}

\item{seed}{Optional integer. Sets the random seed for setup only, so the
starting population is reproducible independently of the run. Note that this
and \code{\link[=abm_run]{abm_run()}}'s \code{seed} do different jobs: this one fixes \emph{who the agents
are}, \code{abm_run()}'s fixes \emph{what happens to them}. A model whose columns are
drawn at random needs both to be reproducible end to end.}
}
\value{
An \code{abm_model} object.
}
\description{
\code{abm_setup()} is the first of the three functions a model is made of --
\code{abm_setup()}, then \code{\link[=abm_go]{abm_go()}}, then \code{\link[=abm_run]{abm_run()}}. It turns declarations into
an initial population: it evaluates the \code{\link[=abm_agents]{abm_agents()}} specifications into
tibbles, builds the \code{\link[=abm_network]{abm_network()}}, and stores the starting values of any
shared globals. The result is the \code{model} argument of \code{\link[=abm_run]{abm_run()}}.
}
\details{
Agent ids (\code{.id}) are unique across the whole model, and every agent carries a
\code{.group} column naming the group it belongs to. A single-group model is given
the group name \code{"agents"}.
}
\examples{
abm_setup(
  agents  = abm_agents(n = 100, threshold = ~runif(n, 40, 80)),
  globals = list(last_attendance = 60)
)

# The world is the first of the three parts a model is made of.
economy <- abm_setup(agents = abm_agents(n = 500, money = 100))

go <- abm_go(
  abm_match(pair = "random", role = list(giver = money > 0, receiver = TRUE)),
  abm_rules(money ~ if_else(.role == "giver", money - 1, money + 1))
)

abm_run(economy, go, ticks = 10, seed = 1)
}
