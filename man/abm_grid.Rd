% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-network.R
\name{abm_grid}
\alias{abm_grid}
\title{A grid of patches, as a single declaration}
\usage{
abm_grid(dims, diagonals = TRUE, torus = TRUE, ...)
}
\arguments{
\item{dims}{\code{c(width, height)}. The cell count is \code{prod(dims)}, and the
wired group inherits it -- do not pass \code{n}.}

\item{diagonals}{\code{TRUE} (the default) for an 8-neighbour Moore lattice,
\code{FALSE} for a 4-neighbour von Neumann one.}

\item{torus}{\code{TRUE} (the default) wraps the edges; \code{FALSE} gives a bounded
grid whose border cells have fewer neighbours.}

\item{...}{Patch column specifications, exactly as \code{\link[=abm_agents]{abm_agents()}} takes them
(\code{n} is available inside a formula and equals \code{prod(dims)}).}
}
\value{
An \code{abm_grid} object, recognised by \code{\link[=abm_setup]{abm_setup()}}.
}
\description{
\code{abm_grid()} is sugar for a patch-heavy model. It desugars to an
\code{\link[=abm_agents]{abm_agents()}} group plus an \code{\link[=abm_network]{abm_network()}} of \code{type = "grid"} wired to it,
so the \code{\link[=abm_go]{abm_go()}} block is identical either way. Use the explicit two-line
form instead when the network should be visible in the setup, or when another
network sits alongside it.
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{patches <- abm_grid(dims = c(w, h), diagonals = FALSE, torus = FALSE,
                    state = ~sample(c("tree", "empty"), n, TRUE))
}\if{html}{\out{</div>}}

desugars to

\if{html}{\out{<div class="sourceCode r">}}\preformatted{agents  = list(patches = abm_agents(state = ~...)),
network = abm_network(type = "grid", dims = c(w, h),
                      diagonals = FALSE, torus = FALSE, on = "patches")
}\if{html}{\out{</div>}}
}
\examples{
life <- abm_setup(
  agents = abm_grid(dims = c(20, 20), alive = ~runif(n) < 0.3)
)
}
\seealso{
\code{\link[=abm_network]{abm_network()}}, \code{\link[=abm_agents]{abm_agents()}}.
}
