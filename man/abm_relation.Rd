% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relation.R
\name{abm_relation}
\alias{abm_relation}
\title{Declare a relation between agents}
\usage{
abm_relation(edges, ...)
}
\arguments{
\item{edges}{A data frame with integer columns \code{from} and \code{to}, the \code{.id}s
of the two agents, and optionally further columns holding each pair's
starting values. \verb{(from, to)} must be unique and \code{from != to}.}

\item{...}{Named default values, one per value column: \code{unmet = 0}. A column
named here but absent from \code{edges} is added at the default; a column in
\code{edges} but not named here defaults to \code{NA} of its type. These defaults are
what \code{\link[=abm_link]{abm_link()}} fills in for a row it creates.}
}
\value{
An \code{abm_relation} object, for \code{abm_setup(relations = )}.
}
\description{
A \emph{relation} is a table of directed pairs of agents, each pair carrying
values of its own. It is where a quantity lives when it belongs to neither
agent alone but to the two of them together: how much this seller has
rationed \emph{this} household, what this bank owes \emph{that} one, how highly I rank
\emph{you}. A model may declare several, by name, in
\code{abm_setup(relations = list(...))}, and they sit alongside the network, which
is unchanged.
}
\details{
The network cannot hold this. It is one per model, it is undirected, and the
only values that attach to its edges come from \code{\link[=abm_draw]{abm_draw()}}, which is a fresh
coin per tick that cannot read its own previous value. A relation is
directed -- \verb{(from, to)} is a different row from \verb{(to, from)} -- and its
values persist until a rule writes them or the row is removed.
}
\section{What a relation gives a rule}{
For a relation named \code{R} with a value column \code{v}, every place that sees a
\emph{pair} of agents also sees:\tabular{ll}{
   name \tab meaning \cr
   \code{.R} \tab \code{TRUE} when the row \code{(me -> them)} exists \cr
   \code{.R_back} \tab \code{TRUE} when \code{(them -> me)} exists \cr
   \code{R_v} \tab the value on \code{(me -> them)}, \code{NA} if no row \cr
   \code{R_v_back} \tab the value on \code{(them -> me)} \cr
}


"Me" is the focal agent and "them" the candidate in \code{\link[=abm_match]{abm_match()}}'s \code{among},
\code{weight} and \code{cost}, and in \code{\link[=abm_neighbours]{abm_neighbours()}}'s \code{within}; under a standing
match in \code{\link[=abm_rules]{abm_rules()}} and \code{\link[=abm_sequential]{abm_sequential()}}, "them" is \code{.partner}. There
\code{R_v ~ expr} \emph{writes} the pair's value, and \code{R_v_back ~ expr} writes the
reverse row. That is the whole point: the number has one home, so two sides
of a transaction cannot drift apart.

Rows are added and removed with \code{\link[=abm_link]{abm_link()}} and \code{\link[=abm_unlink]{abm_unlink()}} using
\code{via = "R"}, updated in bulk with \code{\link[=abm_pairs]{abm_pairs()}}, and read back after a run
with \code{\link[=abm_relations]{abm_relations()}}. \code{\link[=abm_global]{abm_global()}}, measures and \code{\link[=abm_tell]{abm_tell()}} evaluate
over the bare population and do not see relation columns; aggregate through
\code{abm_neighbours(within = .R)} first.
}

\examples{
# each of 3 households buys from 2 of 4 firms (ids 4:7); nothing owed yet
who <- data.frame(from = c(1L, 1L, 2L, 2L, 3L, 3L),
                  to   = c(4L, 5L, 5L, 6L, 6L, 7L))
abm_relation(edges = who, unmet = 0)
}
\seealso{
\code{\link[=abm_setup]{abm_setup()}}, \code{\link[=abm_link]{abm_link()}}, \code{\link[=abm_pairs]{abm_pairs()}}, \code{\link[=abm_relations]{abm_relations()}}.
}
