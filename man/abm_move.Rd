% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-move.R
\name{abm_move}
\alias{abm_move}
\title{Move agents across a lattice}
\usage{
abm_move(
  along,
  to = "random_neighbour",
  who = NULL,
  direction = NULL,
  range = NULL,
  axes_only = FALSE,
  avoid_occupied = FALSE
)
}
\arguments{
\item{along}{Which lattice to move on: the name of the grid-wired group (for
example \code{"patches"}), or \code{"network"} to use the model's edge list as the
adjacency directly.}

\item{to}{How the destination is chosen. \code{"random_neighbour"},
\code{"random_empty_neighbour"}, \code{"stay"}, or \verb{uphill(<expr>)} /
\verb{downhill(<expr>)}. Ignored when \code{direction} is given.}

\item{who}{Character vector of the agent groups that move this step; the
rest of the population is untouched. Defaults to every group that has a
\code{.cell}.}

\item{direction}{A column holding a per-agent heading. The mover steps one
cell along it, rather than choosing a cell by value. Cannot be combined
with \code{to}, \code{range} or \code{axes_only}.}

\item{range}{How many cells out to look. \code{1} (the default) is the adjacent
cells only. Larger values reach further, following the lattice's own
\code{diagonals} and \code{torus} settings.}

\item{axes_only}{Whether to scan only along the four axes (north, south,
east, west) rather than over the whole neighbourhood. Only meaningful with
\code{range > 1}.}

\item{avoid_occupied}{Whether to refuse a cell that another mover is already
standing on. Movers are resolved in a random order and each one claims the
cell it lands on, so two agents never end up on the same cell through this
step.}
}
\value{
An \code{abm_move} step object.
}
\description{
A mobile agent's location is a patch \code{.id} held in a reserved \code{.cell}
column, and moving is writing that column. \code{abm_move()} is the step that
writes it: each mover's \code{.cell} is reassigned to a cell reachable from the
one it is standing on, and \code{.x} / \code{.y} follow automatically.
}
\section{Choosing where to go}{
\code{to} says how the destination is picked among the candidate cells:
\itemize{
\item \code{"random_neighbour"} -- a uniform draw from the adjacent cells.
\item \code{"random_empty_neighbour"} -- the same, but only cells that no mover is
standing on. This is \code{"random_neighbour"} with \code{avoid_occupied = TRUE}.
\item \verb{uphill(<expr>)} / \verb{downhill(<expr>)} -- the candidate cell maximising or
minimising \code{expr}. The cell the agent is already standing on is itself a
candidate, so nothing can push an agent off the best cell in reach.
\item \code{"stay"} -- nobody moves. Useful as the inert branch of a model that
switches movement on and off.
}

Ties are broken at random, and a mover with no legal target stays where it
is rather than erroring. Because the current cell only ties rather than
wins, an agent on flat ground wanders instead of freezing, which is what
keeps a gradient-follower exploring before it finds the gradient. Write
\code{uphill()} over an expression that already prefers the status quo if you
want the strict "only move if it is strictly better" reading.
}

\section{What \code{uphill()} sees}{
\code{uphill()} and \code{downhill()} are recognised forms inside \verb{to =}, not general
functions. The expression is evaluated once per (mover, candidate cell), and
it sees:
\itemize{
\item the \strong{candidate cell's} patch columns under their own names, which is what
makes \code{uphill(chemical)} "step towards more pheromone";
\item the \strong{mover's} own columns under their own names too, so
\code{uphill(if_else(carrying, nest_scent, chemical))} reads as written. Where
both sides have a column of the same name the mover wins, except for the
engine-owned \code{.id}, \code{.group}, \code{.x}, \code{.y} and \code{.cell}, which stay the
candidate cell's;
\item every mover column again as \verb{own_<col>}, so a clash can always be said
explicitly;
\item the globals.
}
}

\section{Looking further than one cell}{
\code{range}, \code{axes_only} and \code{avoid_occupied} widen or narrow the candidate set.
They exist because particular models force them and should be left alone
otherwise: Sugarscape's agents look \code{vision} cells along the four axes and
will not land on an occupied patch, which is
\verb{range = vision, axes_only = TRUE, avoid_occupied = TRUE}.

\code{direction} is the other flavour: instead of picking a neighbour by a value,
the mover steps one cell along a heading it is carrying. Langton's Ant is the
model that forces it. The heading column is an integer \code{0} north, \code{1} east,
\code{2} south, \code{3} west (character names work too), and a heading that points off
a bounded edge leaves the agent where it is.
}

\examples{
# a random walk on the patch lattice
abm_move(along = "patches", to = "random_neighbour", who = c("sheep", "wolves"))

# follow the pheromone home, or up the trail
abm_move(along = "patches", who = "ants",
         to = uphill(dplyr::if_else(carrying, nest_scent, chemical)))

# Sugarscape: look `vision` cells along the axes, skip occupied patches
abm_move(along = "patches", who = "people", to = uphill(psugar),
         range = 6, axes_only = TRUE, avoid_occupied = TRUE)

# Langton's Ant: one cell along a stored heading
abm_move(along = "patches", who = "ant", direction = heading)
}
\seealso{
\code{\link[=abm_go]{abm_go()}}, which lists every step and fixes the order they run in.
\code{\link[=abm_network]{abm_network()}} for the lattice itself.

Other agent update steps:
\code{\link[=abm_global]{abm_global()}},
\code{\link[=abm_neighbours]{abm_neighbours()}},
\code{\link[=abm_rules]{abm_rules()}},
\code{\link[=abm_sequential]{abm_sequential()}},
\code{\link[=abm_tell]{abm_tell()}}
}
\concept{agent update steps}
