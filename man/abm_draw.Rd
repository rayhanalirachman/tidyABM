% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw.R
\name{abm_draw}
\alias{abm_draw}
\title{Attach a value to every edge, visible from both ends}
\usage{
abm_draw(..., .each = c("edge", "endpoint"))
}
\arguments{
\item{...}{One or more \code{name ~ expression} rules, evaluated once per edge.}

\item{.each}{\code{"edge"} for one value per edge, shared by both endpoints;
\code{"endpoint"} for one value per endpoint, read as \code{name} and \code{name_back}.}
}
\value{
An \code{abm_draw} step object.
}
\description{
Two \code{\link[=abm_neighbours]{abm_neighbours()}} passes over the same network draw their random numbers
independently, which is fine when each pass is a separate event and wrong when
it is the \emph{same} event seen from two sides. "How many of my neighbours
punished me" and "how much punishing did I do" have to add up to the same
thing, agent by agent, and they cannot if each aggregate rolls its own dice.
}
\details{
\code{abm_draw()} fixes that by putting the draw on the \strong{edge} rather than in the
aggregate. Each rule is evaluated once per edge, and the value it produces is
then readable inside every later \code{\link[=abm_neighbours]{abm_neighbours()}} rule in the same tick,
from either endpoint, as a column of that name. Because both endpoints read
the same number, two aggregates over it describe the same events.

The right-hand side is evaluated over the \strong{edge table}, one row per
edge, with \code{from} and \code{to}, so \code{n()} is the number of edges and
\code{runif(n())} is "a uniform per edge". Globals are in scope. The values
live until the next \code{abm_draw()} of that name replaces them, so a draw
made once per tick lasts the tick and one made in \code{abm_setup()}'s first
step lasts the run.

\code{.each} says who the value belongs to:
\itemize{
\item \code{"edge"} (the default), one value for the edge. Both endpoints read
the same number in the column of that name. This is a coin the \emph{pair}
tosses: whether they met, what the encounter was worth.
\item \code{"endpoint"}, one value each. The focal agent reads its own in the
column of that name and its neighbour's as \verb{<name>_back}. This is a coin
each of them tosses privately about the other, which is what an asymmetric
interaction needs: whether I noticed \emph{you}, and separately whether you
noticed \emph{me}.
}
}
\examples{
# did this pair meet this tick? Both of them agree on the answer.
abm_go(
  abm_draw(met ~ runif(n()) < 0.3),
  abm_neighbours(partners ~ sum(met))
)

# whether I saw you, and whether you saw me, are two different coins
abm_draw(noticed ~ runif(n()), .each = "endpoint")
}
