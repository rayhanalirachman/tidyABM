% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odd.R
\name{abm_odd}
\alias{abm_odd}
\title{Generate an ODD protocol skeleton}
\usage{
abm_odd(model, go, ticks = NULL)
}
\arguments{
\item{model}{An \code{abm_model} from \code{\link[=abm_setup]{abm_setup()}}.}

\item{go}{An \code{abm_go} sequence.}

\item{ticks}{Optional number of ticks, used for element 2's temporal scale.
Pass the same number you pass \code{\link[=abm_run]{abm_run()}}.}
}
\value{
An \code{abm_odd} object: a named list with one entry per ODD element,
each holding structured fields. \code{print()} renders it as markdown and
\code{\link[=format]{format()}} returns that markdown as a character vector.
}
\description{
The ODD protocol (Overview, Design concepts, Details -- Grimm et al. 2006;
2010; 2020, \emph{JASSS} 23(2):7) is the template a paper describes an ABM in.
\code{abm_odd()} fills in the half of it that the model objects already contain
and marks the other half for you.
}
\details{
It can do that because a tidyABM model is data: \code{\link[=abm_setup]{abm_setup()}} returns the
population and \code{\link[=abm_go]{abm_go()}} returns an ordered list of typed steps, so the
entities and their state variables (element 2), the schedule (element 3), the
initialisation (element 5) and the submodels' equations (element 7) can be
read off them rather than remembered. Three of element 4's design concepts --
interaction, sensing and stochasticity -- are mechanical too, and are
likewise derived.

Everything else is left as \verb{[author input required]}. Purpose, emergence,
adaptation, objectives, learning, prediction, collectives, observation and
the \emph{justification} for each submodel are statements about what the modeller
meant, and no object in the package records intent. In particular this
function never writes that a concept is absent: "no adaptation" is a claim,
and it is yours to make.

Two things follow from that and are worth knowing before you paste the
output into a paper. Expressions are printed verbatim, never restated in
English, because element 7 wants the equations. And nothing here describes
\emph{behaviour}: ODD is the specification, not the results, so no derived text
says what a run does.
}
\examples{
economy <- abm_setup(agents = abm_agents(n = 500, money = 100))
go <- abm_go(
  abm_match(pair = "random", role = list(giver = money > 0, receiver = TRUE)),
  abm_rules(money ~ if_else(.role == "giver", money - 1, money + 1))
)
abm_odd(economy, go, ticks = 1000)
}
\seealso{
\code{\link[=abm_setup]{abm_setup()}} and \code{\link[=abm_go]{abm_go()}}, the two objects this reads.
}
