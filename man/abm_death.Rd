% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/steps.R
\name{abm_death}
\alias{abm_death}
\title{Remove agents}
\usage{
abm_death(when, prune_edges = TRUE)
}
\arguments{
\item{when}{A condition. Agents satisfying it are removed.}

\item{prune_edges}{Whether to delete the removed agents' network edges.
Defaults to \code{TRUE}; set to \code{FALSE} only if you want a network whose node set
deliberately outlives its agents.}
}
\value{
An \code{abm_death} step object.
}
\description{
\code{abm_death()} drops every agent satisfying \code{when}. By default it also removes
those agents' edges from the network, because leaving them in would make
\code{abm_match(pair = "network")} draw partners that no longer exist.
}
\examples{
abm_death(when = resource <= 0)
}
\seealso{
\code{\link[=abm_go]{abm_go()}}, which lists every step and fixes the order they run
in.

Other demographic steps:
\code{\link[=abm_birth]{abm_birth()}}
}
\concept{demographic steps}
