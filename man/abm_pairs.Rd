% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relation.R
\name{abm_pairs}
\alias{abm_pairs}
\title{Update every pair of a relation}
\usage{
abm_pairs(via, ..., .when = NULL)
}
\arguments{
\item{via}{The relation's name, a string.}

\item{...}{One or more \code{value ~ expr} rules.}

\item{.when}{Optional condition, evaluated per row; rows where it does not
hold are left as they are.}
}
\value{
An \code{abm_pairs} step object.
}
\description{
\code{abm_pairs()} is \code{\link[=abm_rules]{abm_rules()}} for a relation: each rule is a \code{value ~ expr}
formula evaluated once per row of the relation named by \code{via}, all rules
simultaneously. It is how a value that belongs to a pair changes for every
pair at once -- interest accruing on every loan, last month's rationing
being forgotten -- with one write and nothing to keep aligned.
}
\details{
Inside a rule the relation's own value columns are visible by name, the two
agents' \code{.id}s as \code{from} and \code{to}, and their agent columns as \verb{from_<col>}
and \verb{to_<col>}. Globals are in scope. A target that is not yet a column of
the relation is created.
}
\examples{
abm_pairs(via = "loans", balance ~ balance * 1.004)
abm_pairs(via = "sellers", unmet ~ 0)
abm_pairs(via = "loans", balance ~ 0, .when = to_bankrupt)
}
\seealso{
\code{\link[=abm_relation]{abm_relation()}}, \code{\link[=abm_rules]{abm_rules()}}, \code{\link[=abm_go]{abm_go()}}.
}
