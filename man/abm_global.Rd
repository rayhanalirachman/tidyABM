% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/steps.R
\name{abm_global}
\alias{abm_global}
\title{Update a shared, population-level value}
\usage{
abm_global(..., .by = NULL)
}
\arguments{
\item{...}{One or more \code{global_name ~ aggregate_expression} rules. The
expression can use agent columns and other globals; each rule sees the
globals as updated by the rules before it in the same call.}

\item{.by}{Optional index. Either a vector of keys or the name of an agent
column whose distinct values are the keys. The global becomes a named
vector, \code{.key} is in scope, and the global's own name refers to that key's
value.}
}
\value{
An \code{abm_global} step object.
}
\description{
\code{abm_global()} writes to a value held once for the whole model rather than
once per agent, El Farol's \code{last_attendance}, a zakah pool, a bank's
ledger. The right-hand side is an aggregate expression evaluated over the agent
tibble, so it normally collapses to a single value.
}
\details{
Unlike the other update steps, \code{abm_global()} does not need a preceding
\code{\link[=abm_match]{abm_match()}}: a population-level summary does not depend on who was paired
with whom.
}
\section{A global indexed by a category}{
Models keep wanting a shared \emph{table} rather than a shared number: a stimulus
per task, a price per good, a queue length per counter. \code{.by} writes one.
The rules are evaluated once per key and the global becomes a \strong{named
vector} indexed by them, which an ordinary rule reads back with
\code{price[good]}.

Two things are in scope during that evaluation and nowhere else. \code{.key} is
the key being written, so \code{sum(task == .key)} is "how many agents are on
\emph{this} task". And the global's own name is bound to \strong{this key's} current
value, not to the whole vector, so an update reads exactly as the scalar
version does:

\if{html}{\out{<div class="sourceCode">}}\preformatted{abm_global(stimulus ~ stimulus + delta - alpha * sum(task == .key) / n(),
           .by = 1:2)
}\if{html}{\out{</div>}}

Each key still sees the \strong{whole population}, \code{n()} is everybody, not
everybody on this task, because that is what a colony-level stimulus balance
means.

\code{.by} names the index either way round. Give it a vector and the index is
declared by the model, which is right when the categories are fixed and a
key with nobody in it still has to be updated. Give it an agent column and
the index is whatever values the agents currently hold, which is right when
the categories come and go. A key the global already has stays in the index
and is still updated even when no agent holds it this tick, so a task nobody
is working on has its stimulus rise rather than dropping out of the table.
}

\examples{
abm_global(last_attendance ~ sum(go_today))

# one stimulus per task, decaying with the number of workers on it
abm_global(stimulus ~ pmax(0, stimulus + 1 - 3 * sum(task == .key) / n()),
           .by = 1:2)
}
