% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiment.R
\name{abm_measures}
\alias{abm_measures}
\title{Measures recorded during a run}
\usage{
abm_measures(x)
}
\arguments{
\item{x}{An \code{abm_result} from \code{\link[=abm_run]{abm_run()}}.}
}
\value{
A tibble with one row per tick per run, or \code{NULL} if the run was made
without \code{measures}.
}
\description{
The table \verb{measures =} built: one row per tick of every run, with the run's
identifying columns in front of it when there was more than one run.
}
\details{
Measures are observations rather than model state, which is what separates
them from \code{\link[=abm_globals]{abm_globals()}}. A global is readable by every rule in the model; a
measure is written to this table and is invisible to the model that produced
it.
}
\examples{
m  <- abm_setup(agents = abm_agents(n = 20, x = ~runif(20)))
go <- abm_go(abm_rules(x ~ x * 0.9))
r  <- abm_run(m, go, ticks = 5, seed = 1,
              measures = list(total = ~sum(x), spread = ~sd(x)))
abm_measures(r)
}
